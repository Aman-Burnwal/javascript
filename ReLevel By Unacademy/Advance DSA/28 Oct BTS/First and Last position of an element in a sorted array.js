// First and Last position of an element in a sorted array


function findElement(arr, x) {
    let left = 0;
    let right = arr.length - 1;


    while (arr <= right) {

        
        
    }
}

const arr = [1, 2, 3, 3, 8, 9, 9, 10, 10, 10, 11];